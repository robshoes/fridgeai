// Placeholder until the first migration exists (Fase 1). Regenerate with:
//   npx supabase gen types typescript --local > src/services/supabase/types.ts
// and re-export `Database` here so `client.ts` can parameterize createClient<Database>.
export {};
